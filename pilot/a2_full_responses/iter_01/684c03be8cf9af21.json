{
 "program_uid": "684c03be8cf9af21",
 "program": "seed-2.0-code#67",
 "iteration": 1,
 "repair_model": "z-ai/glm-5.3-flash",
 "source_code_sha256": "684c03be8cf9af21040868d8b31b78b958786d7fe0b6ea705db8d1e4ed6fde35",
 "prompt_sha256": "2079256db1a4c98def2f3be95017a550e27fdb8ec76383ceaaebfa63f6654b42",
 "status": "ok",
 "finish_reason": "stop",
 "raw_response": "```python\nimport pandas as pd\nimport numpy as np\n\ndef get_features(db: dict, entity_ids, seed_time) -> pd.DataFrame:\n    orders_df = db['orders']\n    order_items_df = db['order_items']\n    reviews_df = db['reviews']\n    payments_df = db['payments']\n\n    def aggregate_product_data(data, suffix):\n        agg_dict = {\n            'order_id': 'nunique',\n            'num_units': ['sum', 'mean', 'max'],\n            'total_product_price': ['sum', 'mean', 'std'],\n            'total_product_freight': ['sum', 'mean'],\n            'total_product_cost': ['sum', 'mean', 'std'],\n            'avg_unit_price': ['mean', 'std', 'min', 'max'],\n            'avg_unit_freight': ['mean', 'std'],\n            'avg_unit_cost': ['mean', 'std', 'min', 'max'],\n            'delivery_days': ['mean', 'std', 'min', 'max'],\n            'has_review': ['sum', 'mean'],\n            'avg_review_score': ['mean', 'std', 'min', 'max'],\n            'total_comment_length': ['sum', 'mean'],\n            'num_comments': ['sum', 'mean'],\n            'total_order_payment': ['mean', 'std'],\n            'num_payments': ['mean', 'sum'],\n            'avg_installments': ['mean', 'std', 'max'],\n        }\n        grouped = data.groupby('product_id').agg(agg_dict)\n        grouped.columns = [f'{col[0]}_{col[1]}_{suffix}' for col in grouped.columns]\n        grouped = grouped.reset_index()\n        return grouped\n\n    # Aggregate order items to (order_id, product_id) level\n    order_items_product_agg = order_items_df.groupby(['order_id', 'product_id']).agg(\n        num_units=('order_item_id', 'count'),\n        total_product_price=('price', 'sum'),\n        total_product_freight=('freight_value', 'sum'),\n        avg_unit_price=('price', 'mean'),\n        avg_unit_freight=('freight_value', 'mean'),\n        order_purchase_ts=('ts', 'first')\n    ).reset_index()\n\n    # Only purchases strictly before seed_time\n    order_items_product_agg = order_items_product_agg[\n        order_items_product_agg['order_purchase_ts'] < seed_time\n    ].copy()\n\n    order_product_with_orders = pd.merge(\n        order_items_product_agg, orders_df, on='order_id', how='left'\n    )\n\n    # Delivery info is only known if the delivery happened before seed_time\n    delivered_known = (\n        order_product_with_orders['order_delivered_customer_date'].notna()\n        & (order_product_with_orders['order_delivered_customer_date'] < seed_time)\n    )\n    order_product_with_orders['delivery_days'] = np.where(\n        delivered_known,\n        (order_product_with_orders['order_delivered_customer_date']\n         - order_product_with_orders['order_purchase_ts']).dt.total_seconds() / (24 * 3600),\n        np.nan\n    )\n    # Carrier handoff known at seed_time\n    carrier_known = (\n        order_product_with_orders['order_delivered_carrier_date'].notna()\n        & (order_product_with_orders['order_delivered_carrier_date'] < seed_time)\n    )\n    order_product_with_orders['carrier_days'] = np.where(\n        carrier_known,\n        (order_product_with_orders['order_delivered_carrier_date']\n         - order_product_with_orders['order_purchase_ts']).dt.total_seconds() / (24 * 3600),\n        np.nan\n    )\n    # Approval known at seed_time\n    approved_known = (\n        order_product_with_orders['order_approved_at'].notna()\n        & (order_product_with_orders['order_approved_at'] < seed_time)\n    )\n    order_product_with_orders['approval_delay_days'] = np.where(\n        approved_known,\n        (order_product_with_orders['order_approved_at']\n         - order_product_with_orders['order_purchase_ts']).dt.total_seconds() / (24 * 3600),\n        np.nan\n    )\n    # is_canceled is only safe if the order was explicitly canceled and we can\n    # observe it: an order that was later delivered/canceled has ambiguous\n    # status history, so we only mark canceled when it is not delivered and\n    # not shipped by seed_time (still an approximation, but conservative).\n    not_delivered = ~(\n        order_product_with_orders['order_delivered_customer_date'].notna()\n        & (order_product_with_orders['order_delivered_customer_date'] < seed_time)\n    )\n    not_shipped = ~carrier_known\n    order_product_with_orders['is_canceled'] = (\n        (order_product_with_orders['order_status'] == 'canceled')\n        & not_delivered & not_shipped\n    ).astype(int)\n\n    order_product_with_orders['total_product_cost'] = (\n        order_product_with_orders['total_product_price']\n        + order_product_with_orders['total_product_freight']\n    )\n    order_product_with_orders['avg_unit_cost'] = (\n        order_product_with_orders['avg_unit_price']\n        + order_product_with_orders['avg_unit_freight']\n    )\n\n    # Reviews: a review becomes visible at answer time; use only reviews\n    # answered before seed_time\n    reviews_pre_seed = reviews_df[\n        reviews_df['review_answer_timestamp'] < seed_time\n    ].copy()\n    reviews_pre_seed['has_comment'] = ~reviews_pre_seed['review_comment_message'].isna()\n    reviews_pre_seed['comment_length'] = reviews_pre_seed['review_comment_message'].fillna('').str.len()\n    reviews_pre_seed['has_title'] = ~reviews_pre_seed['review_comment_title'].isna()\n\n    reviews_per_order = reviews_pre_seed.groupby('order_id').agg(\n        avg_review_score=('review_score', 'mean'),\n        total_comment_length=('comment_length', 'sum'),\n        num_comments=('review_id', 'count'),\n        has_any_comment=('has_comment', 'any'),\n        has_any_title=('has_title', 'any')\n    ).reset_index()\n\n    order_product_with_reviews = pd.merge(\n        order_product_with_orders, reviews_per_order, on='order_id', how='left'\n    )\n    order_product_with_reviews['has_review'] = (\n        order_product_with_reviews['num_comments'].fillna(0) > 0\n    ).astype(int)\n\n    # Payments: only payments recorded before seed_time\n    payments_pre_seed = payments_df[payments_df['ts'] < seed_time].copy()\n    payments_per_order = payments_pre_seed.groupby('order_id').agg(\n        total_order_payment=('payment_value', 'sum'),\n        num_payments=('payment_sequential', 'count'),\n        avg_installments=('payment_installments', 'mean'),\n        max_installments=('payment_installments', 'max'),\n        min_installments=('payment_installments', 'min')\n    ).reset_index()\n\n    order_product_full = pd.merge(\n        order_product_with_reviews, payments_per_order, on='order_id', how='left'\n    )\n\n    # Time windows\n    window_30d = seed_time - pd.Timedelta(days=30)\n    window_90d = seed_time - pd.Timedelta(days=90)\n    window_180d = seed_time - pd.Timedelta(days=180)\n    window_365d = seed_time - pd.Timedelta(days=365)\n\n    ts = order_product_full['order_purchase_ts']\n    data_30d = order_product_full[(ts >= window_30d) & (ts < seed_time)].copy()\n    data_90d = order_product_full[(ts >= window_90d) & (ts < seed_time)].copy()\n    data_180d = order_product_full[(ts >= window_180d) & (ts < seed_time)].copy()\n    data_365d = order_product_full[(ts >= window_365d) & (ts < seed_time)].copy()\n    data_all = order_product_full.copy()\n\n    agg_30d = aggregate_product_data(data_30d, '30d')\n    agg_90d = aggregate_product_data(data_90d, '90d')\n    agg_180d = aggregate_product_data(data_180d, '180d')\n    agg_365d = aggregate_product_data(data_365d, '365d')\n    agg_all = aggregate_product_data(data_all, 'all')\n\n    # Recency / tenure / frequency\n    last_order_per_product = order_product_full.groupby('product_id')['order_purchase_ts'].max().reset_index()\n    last_order_per_product['days_since_last_order'] = (\n        seed_time - last_order_per_product['order_purchase_ts']\n    ).dt.total_seconds() / (24 * 3600)\n    last_order_per_product['days_since_last_order'] = last_order_per_product['days_since_last_order'].round(2)\n\n    first_order_per_product = order_product_full.groupby('product_id')['order_purchase_ts'].min().reset_index()\n    first_order_per_product['product_tenure_days'] = (\n        seed_time - first_order_per_product['order_purchase_ts']\n    ).dt.total_seconds() / (24 * 3600)\n    first_order_per_product['product_tenure_days'] = first_order_per_product['product_tenure_days'].round(2)\n\n    order_count_all = agg_all[['product_id', 'order_id_nunique_all']].copy()\n    product_frequency = pd.merge(\n        order_count_all,\n        first_order_per_product[['product_id', 'product_tenure_days']],\n        on='product_id', how='left'\n    )\n    product_frequency['orders_per_day'] = (\n        product_frequency['order_id_nunique_all'] / (product_frequency['product_tenure_days'] + 1e-6)\n    )\n    product_frequency = product_frequency[['product_id', 'orders_per_day']]\n\n    order_counts_trend = pd.merge(\n        agg_30d[['product_id', 'order_id_nunique_30d']].rename(columns={'order_id_nunique_30d': 'orders_30d'}),\n        agg_90d[['product_id', 'order_id_nunique_90d']].rename(columns={'order_id_nunique_90d': 'orders_90d'}),\n        on='product_id', how='outer'\n    )\n    order_counts_trend = pd.merge(\n        order_counts_trend,\n        agg_180d[['product_id', 'order_id_nunique_180d']].rename(columns={'order_id_nunique_180d': 'orders_180d'}),\n        on='product_id', how='outer'\n    )\n    order_counts_trend = order_counts_trend.fillna(0)\n    order_counts_trend['ratio_30d_to_90d'] = order_counts_trend['orders_30d'] / (order_counts_trend['orders_90d'] + 1e-6)\n    order_counts_trend['ratio_30d_to_180d'] = order_counts_trend['orders_30d'] / (order_counts_trend['orders_180d'] + 1e-6)\n    order_counts_trend['ratio_90d_to_180d'] = (\n        (order_counts_trend['orders_90d'] - order_counts_trend['orders_30d'])\n        / (order_counts_trend['orders_180d'] - order_counts_trend['orders_90d'] + 1e-6)\n    )\n\n    base = pd.DataFrame({'product_id': list(entity_ids)})\n    dfs_to_merge = [\n        agg_all, agg_365d, agg_180d, agg_90d, agg_30d,\n        last_order_per_product[['product_id', 'days_since_last_order']],\n        first_order_per_product[['product_id', 'product_tenure_days']],\n        product_frequency,\n        order_counts_trend\n    ]\n    features_df = base.copy()\n    for df in dfs_to_merge:\n        if not df.empty:\n            features_df = pd.merge(features_df, df, on='product_id', how='left')\n\n    features_df['has_history'] = (~features_df['days_since_last_order'].isna()).astype(int)\n\n    all_cols = [c for c in features_df.columns if c != 'product_id']\n    count_sum_cols = [\n        c for c in all_cols\n        if ('_sum_' in c) or ('_count_' in c) or ('_nunique_' in c) or ('orders_' in c) or ('num_' in c)\n    ]\n    last_order_col = 'days_since_last_order'\n    tenure_col = 'product_tenure_days'\n    remaining_cols = [c for c in all_cols if c not in count_sum_cols and c not in [last_order_col, tenure_col]]\n\n    features_df[count_sum_cols] = features_df[count_sum_cols].fillna(0)\n    features_df[last_order_col] = features_df[last_order_col].fillna(9999)\n    features_df[tenure_col] = features_df[tenure_col].fillna(0)\n    features_df[remaining_cols] = features_df[remaining_cols].fillna(0)\n\n    features_df = features_df.drop_duplicates(subset='product_id')\n    features_df = features_df.set_index('product_id')\n    features_df = features_df.reindex(entity_ids)\n    features_df = features_df.astype(float)\n    return features_df\n```",
 "code": "import pandas as pd\nimport numpy as np\n\ndef get_features(db: dict, entity_ids, seed_time) -> pd.DataFrame:\n    orders_df = db['orders']\n    order_items_df = db['order_items']\n    reviews_df = db['reviews']\n    payments_df = db['payments']\n\n    def aggregate_product_data(data, suffix):\n        agg_dict = {\n            'order_id': 'nunique',\n            'num_units': ['sum', 'mean', 'max'],\n            'total_product_price': ['sum', 'mean', 'std'],\n            'total_product_freight': ['sum', 'mean'],\n            'total_product_cost': ['sum', 'mean', 'std'],\n            'avg_unit_price': ['mean', 'std', 'min', 'max'],\n            'avg_unit_freight': ['mean', 'std'],\n            'avg_unit_cost': ['mean', 'std', 'min', 'max'],\n            'delivery_days': ['mean', 'std', 'min', 'max'],\n            'has_review': ['sum', 'mean'],\n            'avg_review_score': ['mean', 'std', 'min', 'max'],\n            'total_comment_length': ['sum', 'mean'],\n            'num_comments': ['sum', 'mean'],\n            'total_order_payment': ['mean', 'std'],\n            'num_payments': ['mean', 'sum'],\n            'avg_installments': ['mean', 'std', 'max'],\n        }\n        grouped = data.groupby('product_id').agg(agg_dict)\n        grouped.columns = [f'{col[0]}_{col[1]}_{suffix}' for col in grouped.columns]\n        grouped = grouped.reset_index()\n        return grouped\n\n    # Aggregate order items to (order_id, product_id) level\n    order_items_product_agg = order_items_df.groupby(['order_id', 'product_id']).agg(\n        num_units=('order_item_id', 'count'),\n        total_product_price=('price', 'sum'),\n        total_product_freight=('freight_value', 'sum'),\n        avg_unit_price=('price', 'mean'),\n        avg_unit_freight=('freight_value', 'mean'),\n        order_purchase_ts=('ts', 'first')\n    ).reset_index()\n\n    # Only purchases strictly before seed_time\n    order_items_product_agg = order_items_product_agg[\n        order_items_product_agg['order_purchase_ts'] < seed_time\n    ].copy()\n\n    order_product_with_orders = pd.merge(\n        order_items_product_agg, orders_df, on='order_id', how='left'\n    )\n\n    # Delivery info is only known if the delivery happened before seed_time\n    delivered_known = (\n        order_product_with_orders['order_delivered_customer_date'].notna()\n        & (order_product_with_orders['order_delivered_customer_date'] < seed_time)\n    )\n    order_product_with_orders['delivery_days'] = np.where(\n        delivered_known,\n        (order_product_with_orders['order_delivered_customer_date']\n         - order_product_with_orders['order_purchase_ts']).dt.total_seconds() / (24 * 3600),\n        np.nan\n    )\n    # Carrier handoff known at seed_time\n    carrier_known = (\n        order_product_with_orders['order_delivered_carrier_date'].notna()\n        & (order_product_with_orders['order_delivered_carrier_date'] < seed_time)\n    )\n    order_product_with_orders['carrier_days'] = np.where(\n        carrier_known,\n        (order_product_with_orders['order_delivered_carrier_date']\n         - order_product_with_orders['order_purchase_ts']).dt.total_seconds() / (24 * 3600),\n        np.nan\n    )\n    # Approval known at seed_time\n    approved_known = (\n        order_product_with_orders['order_approved_at'].notna()\n        & (order_product_with_orders['order_approved_at'] < seed_time)\n    )\n    order_product_with_orders['approval_delay_days'] = np.where(\n        approved_known,\n        (order_product_with_orders['order_approved_at']\n         - order_product_with_orders['order_purchase_ts']).dt.total_seconds() / (24 * 3600),\n        np.nan\n    )\n    # is_canceled is only safe if the order was explicitly canceled and we can\n    # observe it: an order that was later delivered/canceled has ambiguous\n    # status history, so we only mark canceled when it is not delivered and\n    # not shipped by seed_time (still an approximation, but conservative).\n    not_delivered = ~(\n        order_product_with_orders['order_delivered_customer_date'].notna()\n        & (order_product_with_orders['order_delivered_customer_date'] < seed_time)\n    )\n    not_shipped = ~carrier_known\n    order_product_with_orders['is_canceled'] = (\n        (order_product_with_orders['order_status'] == 'canceled')\n        & not_delivered & not_shipped\n    ).astype(int)\n\n    order_product_with_orders['total_product_cost'] = (\n        order_product_with_orders['total_product_price']\n        + order_product_with_orders['total_product_freight']\n    )\n    order_product_with_orders['avg_unit_cost'] = (\n        order_product_with_orders['avg_unit_price']\n        + order_product_with_orders['avg_unit_freight']\n    )\n\n    # Reviews: a review becomes visible at answer time; use only reviews\n    # answered before seed_time\n    reviews_pre_seed = reviews_df[\n        reviews_df['review_answer_timestamp'] < seed_time\n    ].copy()\n    reviews_pre_seed['has_comment'] = ~reviews_pre_seed['review_comment_message'].isna()\n    reviews_pre_seed['comment_length'] = reviews_pre_seed['review_comment_message'].fillna('').str.len()\n    reviews_pre_seed['has_title'] = ~reviews_pre_seed['review_comment_title'].isna()\n\n    reviews_per_order = reviews_pre_seed.groupby('order_id').agg(\n        avg_review_score=('review_score', 'mean'),\n        total_comment_length=('comment_length', 'sum'),\n        num_comments=('review_id', 'count'),\n        has_any_comment=('has_comment', 'any'),\n        has_any_title=('has_title', 'any')\n    ).reset_index()\n\n    order_product_with_reviews = pd.merge(\n        order_product_with_orders, reviews_per_order, on='order_id', how='left'\n    )\n    order_product_with_reviews['has_review'] = (\n        order_product_with_reviews['num_comments'].fillna(0) > 0\n    ).astype(int)\n\n    # Payments: only payments recorded before seed_time\n    payments_pre_seed = payments_df[payments_df['ts'] < seed_time].copy()\n    payments_per_order = payments_pre_seed.groupby('order_id').agg(\n        total_order_payment=('payment_value', 'sum'),\n        num_payments=('payment_sequential', 'count'),\n        avg_installments=('payment_installments', 'mean'),\n        max_installments=('payment_installments', 'max'),\n        min_installments=('payment_installments', 'min')\n    ).reset_index()\n\n    order_product_full = pd.merge(\n        order_product_with_reviews, payments_per_order, on='order_id', how='left'\n    )\n\n    # Time windows\n    window_30d = seed_time - pd.Timedelta(days=30)\n    window_90d = seed_time - pd.Timedelta(days=90)\n    window_180d = seed_time - pd.Timedelta(days=180)\n    window_365d = seed_time - pd.Timedelta(days=365)\n\n    ts = order_product_full['order_purchase_ts']\n    data_30d = order_product_full[(ts >= window_30d) & (ts < seed_time)].copy()\n    data_90d = order_product_full[(ts >= window_90d) & (ts < seed_time)].copy()\n    data_180d = order_product_full[(ts >= window_180d) & (ts < seed_time)].copy()\n    data_365d = order_product_full[(ts >= window_365d) & (ts < seed_time)].copy()\n    data_all = order_product_full.copy()\n\n    agg_30d = aggregate_product_data(data_30d, '30d')\n    agg_90d = aggregate_product_data(data_90d, '90d')\n    agg_180d = aggregate_product_data(data_180d, '180d')\n    agg_365d = aggregate_product_data(data_365d, '365d')\n    agg_all = aggregate_product_data(data_all, 'all')\n\n    # Recency / tenure / frequency\n    last_order_per_product = order_product_full.groupby('product_id')['order_purchase_ts'].max().reset_index()\n    last_order_per_product['days_since_last_order'] = (\n        seed_time - last_order_per_product['order_purchase_ts']\n    ).dt.total_seconds() / (24 * 3600)\n    last_order_per_product['days_since_last_order'] = last_order_per_product['days_since_last_order'].round(2)\n\n    first_order_per_product = order_product_full.groupby('product_id')['order_purchase_ts'].min().reset_index()\n    first_order_per_product['product_tenure_days'] = (\n        seed_time - first_order_per_product['order_purchase_ts']\n    ).dt.total_seconds() / (24 * 3600)\n    first_order_per_product['product_tenure_days'] = first_order_per_product['product_tenure_days'].round(2)\n\n    order_count_all = agg_all[['product_id', 'order_id_nunique_all']].copy()\n    product_frequency = pd.merge(\n        order_count_all,\n        first_order_per_product[['product_id', 'product_tenure_days']],\n        on='product_id', how='left'\n    )\n    product_frequency['orders_per_day'] = (\n        product_frequency['order_id_nunique_all'] / (product_frequency['product_tenure_days'] + 1e-6)\n    )\n    product_frequency = product_frequency[['product_id', 'orders_per_day']]\n\n    order_counts_trend = pd.merge(\n        agg_30d[['product_id', 'order_id_nunique_30d']].rename(columns={'order_id_nunique_30d': 'orders_30d'}),\n        agg_90d[['product_id', 'order_id_nunique_90d']].rename(columns={'order_id_nunique_90d': 'orders_90d'}),\n        on='product_id', how='outer'\n    )\n    order_counts_trend = pd.merge(\n        order_counts_trend,\n        agg_180d[['product_id', 'order_id_nunique_180d']].rename(columns={'order_id_nunique_180d': 'orders_180d'}),\n        on='product_id', how='outer'\n    )\n    order_counts_trend = order_counts_trend.fillna(0)\n    order_counts_trend['ratio_30d_to_90d'] = order_counts_trend['orders_30d'] / (order_counts_trend['orders_90d'] + 1e-6)\n    order_counts_trend['ratio_30d_to_180d'] = order_counts_trend['orders_30d'] / (order_counts_trend['orders_180d'] + 1e-6)\n    order_counts_trend['ratio_90d_to_180d'] = (\n        (order_counts_trend['orders_90d'] - order_counts_trend['orders_30d'])\n        / (order_counts_trend['orders_180d'] - order_counts_trend['orders_90d'] + 1e-6)\n    )\n\n    base = pd.DataFrame({'product_id': list(entity_ids)})\n    dfs_to_merge = [\n        agg_all, agg_365d, agg_180d, agg_90d, agg_30d,\n        last_order_per_product[['product_id', 'days_since_last_order']],\n        first_order_per_product[['product_id', 'product_tenure_days']],\n        product_frequency,\n        order_counts_trend\n    ]\n    features_df = base.copy()\n    for df in dfs_to_merge:\n        if not df.empty:\n            features_df = pd.merge(features_df, df, on='product_id', how='left')\n\n    features_df['has_history'] = (~features_df['days_since_last_order'].isna()).astype(int)\n\n    all_cols = [c for c in features_df.columns if c != 'product_id']\n    count_sum_cols = [\n        c for c in all_cols\n        if ('_sum_' in c) or ('_count_' in c) or ('_nunique_' in c) or ('orders_' in c) or ('num_' in c)\n    ]\n    last_order_col = 'days_since_last_order'\n    tenure_col = 'product_tenure_days'\n    remaining_cols = [c for c in all_cols if c not in count_sum_cols and c not in [last_order_col, tenure_col]]\n\n    features_df[count_sum_cols] = features_df[count_sum_cols].fillna(0)\n    features_df[last_order_col] = features_df[last_order_col].fillna(9999)\n    features_df[tenure_col] = features_df[tenure_col].fillna(0)\n    features_df[remaining_cols] = features_df[remaining_cols].fillna(0)\n\n    features_df = features_df.drop_duplicates(subset='product_id')\n    features_df = features_df.set_index('product_id')\n    features_df = features_df.reindex(entity_ids)\n    features_df = features_df.astype(float)\n    return features_df",
 "usage": {
  "completion_tokens": 3019,
  "prompt_tokens": 4711,
  "total_tokens": 7730,
  "completion_tokens_details": {
   "accepted_prediction_tokens": null,
   "audio_tokens": 0,
   "reasoning_tokens": 230,
   "rejected_prediction_tokens": null,
   "text_tokens": null,
   "image_tokens": 0
  },
  "prompt_tokens_details": {
   "audio_tokens": 0,
   "cached_tokens": 0,
   "text_tokens": null,
   "image_tokens": null,
   "cache_write_tokens": 0,
   "video_tokens": 0
  },
  "cost": 0.00221615,
  "is_byok": false,
  "cost_details": {
   "upstream_inference_cost": 0.00221615,
   "upstream_inference_prompt_cost": 0.00070665,
   "upstream_inference_completions_cost": 0.0015095
  }
 },
 "wall_seconds": 19.5
}