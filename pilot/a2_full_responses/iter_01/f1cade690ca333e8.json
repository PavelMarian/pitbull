{
 "program_uid": "f1cade690ca333e8",
 "program": "deepseek-v4-flash-0731#79",
 "iteration": 1,
 "repair_model": "z-ai/glm-5.3-flash",
 "source_code_sha256": "f1cade690ca333e8184b3131599353c791128f2c3f3dec5309930022aef5bd30",
 "prompt_sha256": "408f8d3a2153e706416816c127cdd2e5e35249266f6c8856815c1d7b50f4ff4e",
 "status": "ok",
 "finish_reason": "stop",
 "raw_response": "```python\ndef get_features(db: dict, entity_ids, seed_time) -> pd.DataFrame:\n    orders = db[\"orders\"]\n    order_items = db[\"order_items\"]\n    reviews = db[\"reviews\"]\n    payments = db[\"payments\"]\n\n    product_set = set(entity_ids)\n\n    # История покупок: только заказы, совершённые строго до seed_time\n    mask = order_items[\"product_id\"].isin(product_set) & (order_items[\"ts\"] < seed_time)\n    items = order_items.loc[mask, [\"order_id\", \"product_id\", \"seller_id\",\n                                   \"price\", \"freight_value\", \"ts\"]].copy()\n\n    # Пустой результат с фиксированным набором признаков\n    feature_names = [\n        \"order_count\", \"unique_orders\", \"unique_customers\", \"unique_sellers\",\n        \"total_price\", \"avg_price\", \"std_price\", \"max_price\", \"min_price\",\n        \"total_freight\", \"avg_freight\",\n        \"cnt_30\", \"cnt_60\", \"cnt_90\",\n        \"delivered_share_known\", \"in_transit_share\",\n        \"avg_review\", \"std_review\", \"total_reviews\",\n        \"avg_installments\", \"avg_payment_value\",\n        \"credit_share\", \"debit_share\", \"voucher_share\",\n        \"last_purchase_days\", \"first_purchase_days\", \"avg_daily_orders\",\n        \"avg_delivery_days\", \"delivery_delay_days\",\n    ]\n\n    if items.empty:\n        result = pd.DataFrame(0.0, index=list(entity_ids), columns=feature_names)\n        return result.reindex(entity_ids)\n\n    # Информация о заказе: только те заказы, которые уже существуют на seed_time\n    orders_sub = orders.loc[\n        orders[\"order_purchase_timestamp\"] < seed_time,\n        [\"order_id\", \"customer_id\", \"order_status\",\n         \"order_delivered_carrier_date\", \"order_delivered_customer_date\",\n         \"order_estimated_delivery_date\"]\n    ]\n    items = items.merge(orders_sub, on=\"order_id\", how=\"left\")\n\n    # Платежи: учитываем только те, что уже были совершены до seed_time\n    pay = payments.loc[payments[\"ts\"] < seed_time]\n    pay_agg = pay.groupby(\"order_id\").agg(\n        install_mean=(\"payment_installments\", \"mean\"),\n        total_pay=(\"payment_value\", \"sum\"),\n        credit_share=(\"payment_type\", lambda s: (s == \"credit_card\").mean()),\n        debit_share=(\"payment_type\", lambda s: (s == \"debit_card\").mean()),\n        voucher_share=(\"payment_type\", lambda s: (s == \"voucher\").mean()),\n    )\n\n    # Отзывы: только те, что уже опубликованы до seed_time\n    rev = reviews.loc[reviews[\"review_creation_date\"] < seed_time]\n    rev_agg = rev.groupby(\"order_id\").agg(\n        rev_count=(\"review_id\", \"count\"),\n        rev_avg=(\"review_score\", \"mean\"),\n        rev_std=(\"review_score\", \"std\"),\n    )\n\n    items = items.merge(pay_agg, on=\"order_id\", how=\"left\")\n    items = items.merge(rev_agg, on=\"order_id\", how=\"left\")\n\n    # Временные окна\n    items[\"cnt_30\"] = (items[\"ts\"] >= seed_time - pd.Timedelta(days=30)).astype(int)\n    items[\"cnt_60\"] = (items[\"ts\"] >= seed_time - pd.Timedelta(days=60)).astype(int)\n    items[\"cnt_90\"] = (items[\"ts\"] >= seed_time - pd.Timedelta(days=90)).astype(int)\n\n    # Статус/доставку учитываем только если событие уже произошло к seed_time\n    items[\"is_delivered_known\"] = (\n        items[\"order_delivered_customer_date\"].notna()\n        & (items[\"order_delivered_customer_date\"] < seed_time)\n    ).astype(int)\n    items[\"in_transit\"] = (\n        items[\"order_delivered_carrier_date\"].notna()\n        & (items[\"order_delivered_carrier_date\"] < seed_time)\n        & (items[\"is_delivered_known\"] == 0)\n    ).astype(int)\n\n    # Скорость доставки — только для доставленных до seed_time заказов\n    delivered_mask = items[\"is_delivered_known\"] == 1\n    items[\"delivery_days\"] = np.where(\n        delivered_mask,\n        (items[\"order_delivered_customer_date\"] - items[\"ts\"]) / pd.Timedelta(days=1),\n        np.nan,\n    )\n    items[\"delivery_delay\"] = np.where(\n        delivered_mask & items[\"order_estimated_delivery_date\"].notna(),\n        (items[\"order_delivered_customer_date\"] - items[\"order_estimated_delivery_date\"]) / pd.Timedelta(days=1),\n        np.nan,\n    )\n\n    g = items.groupby(\"product_id\")\n    feat = g.agg(\n        order_count=(\"order_id\", \"count\"),\n        unique_orders=(\"order_id\", \"nunique\"),\n        unique_customers=(\"customer_id\", \"nunique\"),\n        unique_sellers=(\"seller_id\", \"nunique\"),\n        total_price=(\"price\", \"sum\"),\n        avg_price=(\"price\", \"mean\"),\n        std_price=(\"price\", \"std\"),\n        max_price=(\"price\", \"max\"),\n        min_price=(\"price\", \"min\"),\n        total_freight=(\"freight_value\", \"sum\"),\n        avg_freight=(\"freight_value\", \"mean\"),\n        cnt_30=(\"cnt_30\", \"sum\"),\n        cnt_60=(\"cnt_60\", \"sum\"),\n        cnt_90=(\"cnt_90\", \"sum\"),\n        delivered_share_known=(\"is_delivered_known\", \"mean\"),\n        in_transit_share=(\"in_transit\", \"mean\"),\n        avg_review=(\"rev_avg\", \"mean\"),\n        std_review=(\"rev_std\", \"mean\"),\n        total_reviews=(\"rev_count\", \"sum\"),\n        avg_installments=(\"install_mean\", \"mean\"),\n        avg_payment_value=(\"total_pay\", \"mean\"),\n        credit_share=(\"credit_share\", \"mean\"),\n        debit_share=(\"debit_share\", \"mean\"),\n        voucher_share=(\"voucher_share\", \"mean\"),\n        avg_delivery_days=(\"delivery_days\", \"mean\"),\n        delivery_delay_days=(\"delivery_delay\", \"mean\"),\n        max_ts=(\"ts\", \"max\"),\n        min_ts=(\"ts\", \"min\"),\n    )\n\n    feat[\"last_purchase_days\"] = (seed_time - feat[\"max_ts\"]) / pd.Timedelta(days=1)\n    feat[\"first_purchase_days\"] = (seed_time - feat[\"min_ts\"]) / pd.Timedelta(days=1)\n    feat[\"avg_daily_orders\"] = feat[\"order_count\"] / feat[\"first_purchase_days\"].clip(lower=1)\n\n    feat = feat.drop(columns=[\"max_ts\", \"min_ts\"])\n\n    result = pd.DataFrame(index=pd.Index(list(entity_ids), name=\"product_id\"))\n    result = result.join(feat, how=\"left\")\n    result = result.reindex(entity_ids)\n    result = result[feature_names].fillna(0.0)\n    return result\n```",
 "code": "def get_features(db: dict, entity_ids, seed_time) -> pd.DataFrame:\n    orders = db[\"orders\"]\n    order_items = db[\"order_items\"]\n    reviews = db[\"reviews\"]\n    payments = db[\"payments\"]\n\n    product_set = set(entity_ids)\n\n    # История покупок: только заказы, совершённые строго до seed_time\n    mask = order_items[\"product_id\"].isin(product_set) & (order_items[\"ts\"] < seed_time)\n    items = order_items.loc[mask, [\"order_id\", \"product_id\", \"seller_id\",\n                                   \"price\", \"freight_value\", \"ts\"]].copy()\n\n    # Пустой результат с фиксированным набором признаков\n    feature_names = [\n        \"order_count\", \"unique_orders\", \"unique_customers\", \"unique_sellers\",\n        \"total_price\", \"avg_price\", \"std_price\", \"max_price\", \"min_price\",\n        \"total_freight\", \"avg_freight\",\n        \"cnt_30\", \"cnt_60\", \"cnt_90\",\n        \"delivered_share_known\", \"in_transit_share\",\n        \"avg_review\", \"std_review\", \"total_reviews\",\n        \"avg_installments\", \"avg_payment_value\",\n        \"credit_share\", \"debit_share\", \"voucher_share\",\n        \"last_purchase_days\", \"first_purchase_days\", \"avg_daily_orders\",\n        \"avg_delivery_days\", \"delivery_delay_days\",\n    ]\n\n    if items.empty:\n        result = pd.DataFrame(0.0, index=list(entity_ids), columns=feature_names)\n        return result.reindex(entity_ids)\n\n    # Информация о заказе: только те заказы, которые уже существуют на seed_time\n    orders_sub = orders.loc[\n        orders[\"order_purchase_timestamp\"] < seed_time,\n        [\"order_id\", \"customer_id\", \"order_status\",\n         \"order_delivered_carrier_date\", \"order_delivered_customer_date\",\n         \"order_estimated_delivery_date\"]\n    ]\n    items = items.merge(orders_sub, on=\"order_id\", how=\"left\")\n\n    # Платежи: учитываем только те, что уже были совершены до seed_time\n    pay = payments.loc[payments[\"ts\"] < seed_time]\n    pay_agg = pay.groupby(\"order_id\").agg(\n        install_mean=(\"payment_installments\", \"mean\"),\n        total_pay=(\"payment_value\", \"sum\"),\n        credit_share=(\"payment_type\", lambda s: (s == \"credit_card\").mean()),\n        debit_share=(\"payment_type\", lambda s: (s == \"debit_card\").mean()),\n        voucher_share=(\"payment_type\", lambda s: (s == \"voucher\").mean()),\n    )\n\n    # Отзывы: только те, что уже опубликованы до seed_time\n    rev = reviews.loc[reviews[\"review_creation_date\"] < seed_time]\n    rev_agg = rev.groupby(\"order_id\").agg(\n        rev_count=(\"review_id\", \"count\"),\n        rev_avg=(\"review_score\", \"mean\"),\n        rev_std=(\"review_score\", \"std\"),\n    )\n\n    items = items.merge(pay_agg, on=\"order_id\", how=\"left\")\n    items = items.merge(rev_agg, on=\"order_id\", how=\"left\")\n\n    # Временные окна\n    items[\"cnt_30\"] = (items[\"ts\"] >= seed_time - pd.Timedelta(days=30)).astype(int)\n    items[\"cnt_60\"] = (items[\"ts\"] >= seed_time - pd.Timedelta(days=60)).astype(int)\n    items[\"cnt_90\"] = (items[\"ts\"] >= seed_time - pd.Timedelta(days=90)).astype(int)\n\n    # Статус/доставку учитываем только если событие уже произошло к seed_time\n    items[\"is_delivered_known\"] = (\n        items[\"order_delivered_customer_date\"].notna()\n        & (items[\"order_delivered_customer_date\"] < seed_time)\n    ).astype(int)\n    items[\"in_transit\"] = (\n        items[\"order_delivered_carrier_date\"].notna()\n        & (items[\"order_delivered_carrier_date\"] < seed_time)\n        & (items[\"is_delivered_known\"] == 0)\n    ).astype(int)\n\n    # Скорость доставки — только для доставленных до seed_time заказов\n    delivered_mask = items[\"is_delivered_known\"] == 1\n    items[\"delivery_days\"] = np.where(\n        delivered_mask,\n        (items[\"order_delivered_customer_date\"] - items[\"ts\"]) / pd.Timedelta(days=1),\n        np.nan,\n    )\n    items[\"delivery_delay\"] = np.where(\n        delivered_mask & items[\"order_estimated_delivery_date\"].notna(),\n        (items[\"order_delivered_customer_date\"] - items[\"order_estimated_delivery_date\"]) / pd.Timedelta(days=1),\n        np.nan,\n    )\n\n    g = items.groupby(\"product_id\")\n    feat = g.agg(\n        order_count=(\"order_id\", \"count\"),\n        unique_orders=(\"order_id\", \"nunique\"),\n        unique_customers=(\"customer_id\", \"nunique\"),\n        unique_sellers=(\"seller_id\", \"nunique\"),\n        total_price=(\"price\", \"sum\"),\n        avg_price=(\"price\", \"mean\"),\n        std_price=(\"price\", \"std\"),\n        max_price=(\"price\", \"max\"),\n        min_price=(\"price\", \"min\"),\n        total_freight=(\"freight_value\", \"sum\"),\n        avg_freight=(\"freight_value\", \"mean\"),\n        cnt_30=(\"cnt_30\", \"sum\"),\n        cnt_60=(\"cnt_60\", \"sum\"),\n        cnt_90=(\"cnt_90\", \"sum\"),\n        delivered_share_known=(\"is_delivered_known\", \"mean\"),\n        in_transit_share=(\"in_transit\", \"mean\"),\n        avg_review=(\"rev_avg\", \"mean\"),\n        std_review=(\"rev_std\", \"mean\"),\n        total_reviews=(\"rev_count\", \"sum\"),\n        avg_installments=(\"install_mean\", \"mean\"),\n        avg_payment_value=(\"total_pay\", \"mean\"),\n        credit_share=(\"credit_share\", \"mean\"),\n        debit_share=(\"debit_share\", \"mean\"),\n        voucher_share=(\"voucher_share\", \"mean\"),\n        avg_delivery_days=(\"delivery_days\", \"mean\"),\n        delivery_delay_days=(\"delivery_delay\", \"mean\"),\n        max_ts=(\"ts\", \"max\"),\n        min_ts=(\"ts\", \"min\"),\n    )\n\n    feat[\"last_purchase_days\"] = (seed_time - feat[\"max_ts\"]) / pd.Timedelta(days=1)\n    feat[\"first_purchase_days\"] = (seed_time - feat[\"min_ts\"]) / pd.Timedelta(days=1)\n    feat[\"avg_daily_orders\"] = feat[\"order_count\"] / feat[\"first_purchase_days\"].clip(lower=1)\n\n    feat = feat.drop(columns=[\"max_ts\", \"min_ts\"])\n\n    result = pd.DataFrame(index=pd.Index(list(entity_ids), name=\"product_id\"))\n    result = result.join(feat, how=\"left\")\n    result = result.reindex(entity_ids)\n    result = result[feature_names].fillna(0.0)\n    return result",
 "usage": {
  "completion_tokens": 1668,
  "prompt_tokens": 3302,
  "total_tokens": 4970,
  "completion_tokens_details": {
   "accepted_prediction_tokens": null,
   "audio_tokens": 0,
   "reasoning_tokens": 185,
   "rejected_prediction_tokens": null,
   "text_tokens": null,
   "image_tokens": 0
  },
  "prompt_tokens_details": {
   "audio_tokens": 0,
   "cached_tokens": 0,
   "text_tokens": null,
   "image_tokens": null,
   "cache_write_tokens": 0,
   "video_tokens": 0
  },
  "cost": 0.0013293,
  "is_byok": false,
  "cost_details": {
   "upstream_inference_cost": 0.0013293,
   "upstream_inference_prompt_cost": 0.0004953,
   "upstream_inference_completions_cost": 0.000834
  }
 },
 "wall_seconds": 10.0
}