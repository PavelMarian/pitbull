{
  "_comment": "Availability map used for every measurement reported in background.md and docs/results.md -- kept here as a worked example, not as a default baked into the code. Point --availability-map at a copy of this (or your own) when checking against the Olist corpus.",
  "time_cols": {
    "orders": "order_purchase_timestamp",
    "order_items": "ts",
    "reviews": "review_creation_date",
    "payments": "ts"
  },
  "self_availability_cols": {
    "orders": ["order_approved_at", "order_delivered_carrier_date", "order_delivered_customer_date"],
    "reviews": ["review_answer_timestamp"]
  },
  "gatekeeper_cols": {},
  "key_hints": ["_id", "id", "zip", "prefix", "order_item"]
}
