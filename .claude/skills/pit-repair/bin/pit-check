#!/usr/bin/env bash
# Wrapper over scripts/pit_check.py. Interpreter: $PIT_PYTHON, else python3.
HERE="$(cd "$(dirname "$(readlink -f "$0")")" && pwd)"
exec "${PIT_PYTHON:-python3}" "$HERE/../scripts/pit_check.py" "$@"
